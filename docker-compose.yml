version: '3.9'

services:
  app:
    image: backend-engineering-case-study:1.0
    container_name: backend-engineering-case-study
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8080:8080'
    networks:
      - 'local-network'
    environment:
      - DB_PASSWORD=${MYSQL_ROOT_PASSWORD}
    depends_on:
      - mysqldb
      - redis

  mysqldb:
    image: mysql:8.0.28-oracle
    container_name: case-mysqldb
    restart: always
    environment:
      MYSQL_DATABASE: 'mysql-db'
      MYSQL_ROOT_USERNAME: 'root'
      MYSQL_PASSWORD: 'Bavkcr58-'
      MYSQL_ROOT_PASSWORD: 'Bavkcr58-'
    networks:
      - 'local-network'
    ports:
      - '3306:3306'
    volumes:
      - case-mysql-data:/var/lib/mysql
      - ./mysql-db-dump.sql:/docker-entrypoint-initdb.d/mysql-db-dump.sql:ro

  redis:
    image: redis:latest
    container_name: redis-server
    ports:
      - '6379:6379'
    networks:
      - 'local-network'

volumes:
  case-mysql-data:

networks:
  local-network:
    driver: bridge
